import { SiDotnet, SiReact, SiVuedotjs, SiPostgresql, SiRedis, SiMongodb, SiTypescript, SiCsharp, SiSqlite, SiRubygems } from 'react-icons/si';
import { useInView } from 'framer-motion';
import { useRef } from 'react';

export default function Projects() {
    return (
        <section id="projects" className="flex items-center flex-col flex-wrap gap-8 justify-center max-w-5xl mx-auto min-h-screen">
            <div>
            </div>
        </section>)
}
